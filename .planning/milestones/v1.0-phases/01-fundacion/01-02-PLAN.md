---
phase: 01-fundacion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/router.tsx
  - src/main.tsx
  - src/App.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/SidebarUnit.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-07]

must_haves:
  truths:
    - "El sidebar muestra 6 unidades con U1 visualmente activa y U2-U6 grayed out con icono de candado"
    - "Click en unidad bloqueada no navega — muestra tooltip 'Proximamente' o no hace nada"
    - "El header muestra 'BDD Lab UTFSM — Ayudantia INF-239' y es sticky al scrollear"
    - "El footer muestra 'BDD Lab UTFSM - Ayudantia INF-239 - 2026'"
    - "Tareas, Certamenes y Ayudantias aparecen en seccion separada del sidebar con estilo bloqueado"
    - "La URL cambia al navegar entre secciones de U1"
  artifacts:
    - path: "src/router.tsx"
      provides: "React Router v7 route definitions"
      exports: ["router"]
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Root layout with sidebar + header + main + footer"
      exports: ["default"]
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar component reading from UNITS data"
      exports: ["default"]
    - path: "src/components/layout/SidebarUnit.tsx"
      provides: "Individual unit item with expand/collapse and lock state"
      exports: ["default"]
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header with branding"
      exports: ["default"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with credits"
      exports: ["default"]
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/data/units.ts"
      via: "import UNITS, SIDEBAR_EXTRA"
      pattern: "import.*UNITS.*from.*data/units"
    - from: "src/components/layout/SidebarUnit.tsx"
      to: "react-router"
      via: "NavLink for active section highlighting"
      pattern: "NavLink"
    - from: "src/main.tsx"
      to: "src/router.tsx"
      via: "RouterProvider renders the router"
      pattern: "RouterProvider"
    - from: "src/components/layout/AppLayout.tsx"
      to: "react-router"
      via: "Outlet renders child routes"
      pattern: "Outlet"
---

<objective>
Implementar el layout shell completo: AppLayout con Sidebar, Header y Footer integrados con React Router v7. El sidebar muestra las 6 unidades desde el data layer (U1 activa, U2-U6 bloqueadas) y los items extra (Tareas, Certamenes, Ayudantias) en seccion separada.

Purpose: Crear la estructura de navegacion funcional que el estudiante usara para moverse por la plataforma. Es el esqueleto visual sobre el que se monta todo el contenido.
Output: Layout shell navegable con sidebar data-driven, header sticky, footer, y rutas funcionales.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacion/01-RESEARCH.md
@.planning/phases/01-fundacion/01-01-SUMMARY.md

<interfaces>
<!-- Tipos y exports del plan 01 que este plan consume -->

From src/data/units.ts:
```typescript
export interface UnitSection {
  id: SectionId;
  label: string;
  icon: string;
}

export interface Unit {
  id: string;
  number: number;
  title: string;
  locked: boolean;
  sections: UnitSection[];
}

export const SECTIONS: UnitSection[];
export const UNITS: Unit[];
export const SIDEBAR_EXTRA: { id: string; label: string; icon: string; locked: true }[];
```

From src/types/content.ts:
```typescript
export type SectionId = 'conceptos' | 'ejercicios' | 'editor-sql' | 'quiz' | 'cheat-sheet';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router + AppLayout + Header + Footer</name>
  <files>src/router.tsx, src/main.tsx, src/App.tsx, src/components/layout/AppLayout.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx</files>
  <action>
1. Crear `src/router.tsx`:
   - Importar `createBrowserRouter` de `react-router` (NO de `react-router-dom`)
   - Importar `AppLayout`, crear placeholder temporal para `InfoGeneral` y `SectionPage` (inline components que muestren texto basico — se reemplazan en plan 03)
   - Rutas:
     - Layout route sin path: `element: <AppLayout />`
     - Index route: `element: <InfoGeneral />` (placeholder "Informacion General")
     - `path: 'unit/:unitId/:section'`: `element: <SectionPage />` (placeholder que muestre unitId y section de params)
   - Exportar `router`

2. Actualizar `src/main.tsx`:
   - Reemplazar render de `<App />` por `<RouterProvider router={router} />`
   - Importar `RouterProvider` de `react-router` y `router` de `./router`
   - Mantener imports de fontsource y css

3. `src/App.tsx` puede eliminarse o vaciarse — ya no se usa.

4. Crear `src/components/layout/AppLayout.tsx`:
   - Estado del sidebar: `isOpen` (boolean, mobile overlay), `collapsed` (boolean, desktop collapse)
   - Estado inicial: `isOpen: false`, `collapsed: false`
   - Layout flex horizontal: Sidebar | Content column
   - Content column: flex vertical con Header, `<main>` con `<Outlet />`, Footer
   - Main: `flex-1 overflow-auto p-6`
   - Pasar al Sidebar: `isOpen`, `collapsed`, `onToggleMobile` (toggle isOpen), `onToggleCollapse` (toggle collapsed), `onCloseMobile` (set isOpen false)
   - Pasar al Header: `onOpenMobile` (set isOpen true)
   - Overlay backdrop en mobile: div con bg-black/50 que cubre todo cuando isOpen=true, onClick cierra sidebar

5. Crear `src/components/layout/Header.tsx`:
   - Props: `onOpenMobile: () => void`
   - Sticky: `sticky top-0 z-30`
   - Fondo: `bg-zinc-900 border-b border-zinc-800`
   - Contenido: "BDD Lab UTFSM" como titulo principal + " — Ayudantia INF-239" como subtexto mas tenue
   - Boton hamburger (Menu icon de lucide-react): visible solo en mobile (`lg:hidden`), llama `onOpenMobile`
   - Padding horizontal consistente con el contenido

6. Crear `src/components/layout/Footer.tsx`:
   - Sin props
   - Una linea centrada: "BDD Lab UTFSM - Ayudantia INF-239 - 2026"
   - Fondo: `bg-zinc-900 border-t border-zinc-800`
   - Texto: `text-zinc-500 text-sm`
   - Padding vertical minimo (py-3)

Todo en espanol (NFR-05). Estilo dark mode con palette zinc (NFR-01 — fondos zinc-950/zinc-900, texto zinc-100/zinc-400, bordes zinc-800). Acento indigo-500 para highlights per decisiones de usuario.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>El layout shell renderiza: header sticky con branding "BDD Lab UTFSM — Ayudantia INF-239", area de contenido con Outlet, footer con creditos. La URL cambia al navegar. `npx tsc --noEmit` pasa sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar con unidades, lock states y seccion extra</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/SidebarUnit.tsx</files>
  <action>
1. Crear `src/components/layout/Sidebar.tsx`:
   - Props: `isOpen: boolean`, `collapsed: boolean`, `onToggleMobile: () => void`, `onToggleCollapse: () => void`, `onCloseMobile: () => void`
   - Importar `UNITS` y `SIDEBAR_EXTRA` de `@/data/units`
   - Clases responsive siguiendo Pattern 3 de RESEARCH.md:
     - Base: `fixed inset-y-0 left-0 z-40 flex flex-col bg-zinc-900 border-r border-zinc-800 transition-transform duration-200`
     - Mobile: `isOpen ? 'translate-x-0' : '-translate-x-full'`
     - Desktop: `lg:relative lg:translate-x-0`
     - Ancho: `collapsed ? 'w-16' : 'w-64'`
   - Header del sidebar: logo/titulo "BDD Lab" + boton de collapse (ChevronLeft/ChevronRight de lucide) visible solo en desktop (`hidden lg:flex`)
   - Seccion principal: iterar `UNITS` con `<SidebarUnit>` para cada una
   - Separador visual: `<div className="border-t border-zinc-800 my-2" />`
   - Seccion extra: iterar `SIDEBAR_EXTRA`, renderizar cada item con icono + label + icono Lock, estilo grayed out (`text-zinc-600`), sin interaccion (no link, no click handler)
   - Cuando `collapsed=true`: mostrar solo iconos, ocultar labels con `overflow-hidden` y width transition
   - Footer del sidebar: espacio flexible para empujar la seccion extra hacia abajo

2. Crear `src/components/layout/SidebarUnit.tsx`:
   - Props: `unit: Unit`, `collapsed: boolean`, `onNavigate: () => void` (para cerrar sidebar en mobile al seleccionar)
   - Si `unit.locked`:
     - Render grayed out: `text-zinc-600` con icono Lock de lucide-react
     - Click: no hace nada (no Link, no navigate). Opcionalmente mostrar tooltip "Proximamente" con title attribute
     - No mostrar sub-secciones
   - Si `unit.locked === false`:
     - Estado local `expanded: boolean` (default true para la unica unidad activa)
     - Click en header de unidad: toggle expanded
     - Icono ChevronDown/ChevronRight segun expanded
     - Sub-secciones: iterar `unit.sections`, cada una como `<NavLink>` de react-router:
       - `to={/unit/${unit.id}/${section.id}}`
       - Clase activa: `bg-indigo-500/10 text-indigo-400 font-medium` (per RESEARCH.md Pattern NavLink)
       - Clase inactiva: `text-zinc-400 hover:text-zinc-100 hover:bg-zinc-800`
       - Icono de lucide-react basado en `section.icon` (usar dynamic import o mapa de iconos)
     - Al hacer click en una sub-seccion, llamar `onNavigate()` para cerrar overlay en mobile
   - Cuando `collapsed=true`: solo mostrar icono de la unidad (BookOpen por ejemplo), sin texto ni sub-secciones

Para los iconos de lucide: crear un mapa `{ [key: string]: LucideIcon }` importando los iconos especificos (BookOpen, PenTool, Terminal, CheckSquare, FileText, Lock, ChevronDown, ChevronRight, ClipboardList, FileCheck, Users, Menu, ChevronLeft). NO hacer dynamic import de lucide — importar los iconos nombrados directamente y mapearlos.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Sidebar muestra 6 unidades: U1 con sub-secciones expandibles y NavLink activo, U2-U6 grayed out con icono de candado. Seccion inferior muestra Tareas, Certamenes, Ayudantias bloqueados con separador visual. Click en sub-seccion de U1 navega y la URL cambia. Click en unidad bloqueada no hace nada. `npx tsc --noEmit` pasa.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` compila sin errores
2. Sidebar muestra 6 unidades leyendo de `UNITS` (data-driven)
3. U1 tiene sub-secciones expandibles, U2-U6 tienen icono de candado
4. Click en sub-seccion de U1 navega a `/unit/u1/conceptos` (etc.) y NavLink se resalta
5. Click en unidad bloqueada no navega
6. Header es sticky con branding correcto
7. Footer muestra creditos
8. Tareas/Certamenes/Ayudantias aparecen en seccion separada del sidebar
</verification>

<success_criteria>
Layout shell completo y navegable. El estudiante ve el sidebar con todas las unidades (U1 activa, U2-U6 bloqueadas), puede navegar sub-secciones de U1, ve header con branding y footer con creditos. Toda la navegacion es data-driven desde src/data/units.ts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacion/01-02-SUMMARY.md`
</output>
