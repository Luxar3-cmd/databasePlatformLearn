---
phase: 01-fundacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/data/units.ts
  - src/types/content.ts
autonomous: true
requirements: [NFR-01, NFR-03, NFR-05]

must_haves:
  truths:
    - "npm run dev arranca el servidor sin errores"
    - "El browser muestra una pagina con fondo oscuro (zinc-950) y texto claro"
    - "Las fuentes Inter y JetBrains Mono se cargan correctamente"
    - "El archivo src/data/units.ts exporta 6 unidades con U1 desbloqueada y U2-U6 bloqueadas"
    - "Agregar una unidad al array UNITS no requiere tocar ningun componente"
  artifacts:
    - path: "src/data/units.ts"
      provides: "Data-driven navigation source of truth"
      exports: ["Unit", "UnitSection", "SECTIONS", "UNITS", "SIDEBAR_EXTRA"]
    - path: "src/types/content.ts"
      provides: "TypeScript types for content system"
      exports: ["SectionId"]
    - path: "src/index.css"
      provides: "Tailwind v4 setup with dark mode and fonts"
      contains: "@custom-variant dark"
    - path: "vite.config.ts"
      provides: "Vite config with React + Tailwind v4 plugins"
    - path: "index.html"
      provides: "HTML entry with dark class and lang=es"
      contains: 'class="dark"'
  key_links:
    - from: "index.html"
      to: "src/index.css"
      via: "Tailwind v4 dark mode class on html + @custom-variant in CSS"
      pattern: 'class="dark"'
    - from: "src/index.css"
      to: "vite.config.ts"
      via: "@tailwindcss/vite plugin processes CSS"
      pattern: "@import \"tailwindcss\""
---

<objective>
Inicializar el proyecto Vite + React 19 + TypeScript + Tailwind v4 desde cero, configurar dark mode permanente con tipografia Inter/JetBrains Mono, y crear la capa de datos que define las unidades y secciones de la plataforma.

Purpose: Establecer la base tecnica y el data layer que todos los componentes de la fase 1 consumiran. Sin esto no hay proyecto.
Output: Proyecto funcional con `npm run dev`, dark mode activo, y `src/data/units.ts` como fuente de verdad para la navegacion.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundacion/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold proyecto Vite + React 19 + Tailwind v4 + dark mode</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/App.tsx, src/index.css</files>
  <action>
1. Ejecutar `npm create vite@latest . -- --template react-ts` en el directorio del proyecto (ya existe `documentos-clase/`, usar `.` para scaffold in-place).

2. Instalar dependencias:
   ```bash
   npm install tailwindcss @tailwindcss/vite react-router lucide-react
   npm install @fontsource/inter @fontsource/jetbrains-mono
   ```

3. Configurar `vite.config.ts`:
   - Plugins: `react()` y `tailwindcss()` de `@tailwindcss/vite`
   - Alias `@` -> `./src` con `path.resolve`

4. Configurar `index.html`:
   - `<html lang="es" class="dark">`
   - Title: "BDD Lab UTFSM"

5. Configurar `src/index.css` (CRITICO -- seguir exactamente la sintaxis v4):
   ```css
   @import "tailwindcss";
   @custom-variant dark (&:where(.dark, .dark *));
   @theme {
     --font-sans: 'Inter', ui-sans-serif, system-ui, sans-serif;
     --font-mono: 'JetBrains Mono', ui-monospace, monospace;
   }
   ```
   Mas `@layer base` para aplicar font-family al body y code/pre.

6. En `src/main.tsx`: importar `@fontsource/inter` y `@fontsource/jetbrains-mono`, importar `index.css`, render basico de `<App />`.

7. `src/App.tsx` temporal: un `<div>` con clases `bg-zinc-950 text-zinc-100 h-screen flex items-center justify-center` mostrando "BDD Lab UTFSM" en texto grande. Esto se reemplaza en plan 02 con el router.

NO usar `tailwind.config.js` -- no existe en v4. NO usar `@tailwind base/components/utilities` -- en v4 es `@import "tailwindcss"`. NO importar de `react-router-dom` -- en v7 es solo `react-router`.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npm run dev -- --host 2>&1 | head -20 &amp; sleep 5 &amp;&amp; curl -s http://localhost:5173 | grep -q "BDD Lab" &amp;&amp; echo "PASS: Dev server running and serving content" || echo "FAIL"; kill %1 2>/dev/null</automated>
  </verify>
  <done>`npm run dev` arranca sin errores. El browser muestra fondo zinc-950 con texto claro "BDD Lab UTFSM". La fuente Inter se aplica al texto.</done>
</task>

<task type="auto">
  <name>Task 2: Crear data layer y tipos TypeScript para navegacion</name>
  <files>src/data/units.ts, src/types/content.ts</files>
  <action>
1. Crear `src/types/content.ts`:
   - Exportar type literal `SectionId = 'conceptos' | 'ejercicios' | 'editor-sql' | 'quiz' | 'cheat-sheet'`
   - Este type se usara en fases posteriores para mapear secciones a componentes de contenido

2. Crear `src/data/units.ts` siguiendo EXACTAMENTE el patron de RESEARCH.md (Pattern 1):
   - Interface `UnitSection` con `id: SectionId`, `label: string`, `icon: string` (nombre de lucide icon)
   - Interface `Unit` con `id: string`, `number: number`, `title: string`, `locked: boolean`, `sections: UnitSection[]`
   - Constante `SECTIONS: UnitSection[]` con las 5 secciones: Conceptos (BookOpen), Ejercicios (PenTool), Editor SQL (Terminal), Quiz (CheckSquare), Cheat Sheet (FileText)
   - Constante `UNITS: Unit[]` con 6 unidades:
     - U1: "Introduccion a Bases de Datos" (locked: false)
     - U2: "Modelo Entidad-Relacion" (locked: true)
     - U3: "Modelo Relacional" (locked: true)
     - U4: "SQL" (locked: true)
     - U5: "Normalizacion" (locked: true)
     - U6: "Temas Avanzados" (locked: true)
   - Constante `SIDEBAR_EXTRA` con items bloqueados: Tareas (ClipboardList), Certamenes (FileCheck), Ayudantias (Users)
   - Todos los labels en espanol (NFR-05)

3. Crear estructura de carpetas vacia para fases futuras:
   - `src/components/layout/` (para plan 02)
   - `src/components/ui/` (para plan 02-03)
   - `src/pages/` (para plan 02-03)
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>`npx tsc --noEmit` pasa sin errores. `src/data/units.ts` exporta UNITS (6 unidades, U1 unlocked, U2-U6 locked), SECTIONS (5 secciones), SIDEBAR_EXTRA (3 items locked). Los tipos Unit y UnitSection estan exportados y son consumibles por componentes futuros.</done>
</task>

</tasks>

<verification>
1. `npm run dev` arranca sin errores y sirve la pagina
2. El HTML renderizado tiene `<html lang="es" class="dark">`
3. El fondo es zinc-950 (dark mode activo via @custom-variant)
4. `npx tsc --noEmit` compila sin errores de tipos
5. `src/data/units.ts` exporta UNITS con 6 elementos
6. No existe `tailwind.config.js` (v4 no lo usa)
</verification>

<success_criteria>
Proyecto funcional con dark mode permanente, tipografia configurada, y data layer completo que sirve como fuente de verdad para la navegacion de toda la plataforma.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacion/01-01-SUMMARY.md`
</output>
