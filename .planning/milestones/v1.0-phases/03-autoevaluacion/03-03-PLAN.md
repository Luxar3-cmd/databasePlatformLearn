---
phase: 03-autoevaluacion
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/content/units/u1/solved-exercises.ts
  - src/components/u1/SolvedExerciseCard.tsx
  - src/components/u1/SolvedExercisesSection.tsx
  - src/components/u1/ExercisesSection.tsx
autonomous: true
requirements: [EJER-01, EJER-02, EJER-03, EJER-04]

must_haves:
  truths:
    - "El estudiante puede trabajar 8 ejercicios resueltos de 4 tipos con revelacion progresiva paso a paso"
    - "Los ejercicios resueltos usan escenarios de vida real (UTFSM, Spotify, Netflix, etc.)"
    - "Los ejercicios resueltos son accesibles desde la seccion Ejercicios junto con los SQL"
  artifacts:
    - path: "src/content/units/u1/solved-exercises.ts"
      provides: "8 ejercicios resueltos tipados (2 por tipo, 4 tipos)"
      exports: ["SolvedStep", "SolvedExercise", "U1_SOLVED_EXERCISES"]
    - path: "src/components/u1/SolvedExerciseCard.tsx"
      provides: "Card con revelacion progresiva paso a paso"
      exports: ["SolvedExerciseCard"]
    - path: "src/components/u1/SolvedExercisesSection.tsx"
      provides: "Lista agrupada por tipo con SolvedExerciseCards"
      exports: ["default"]
    - path: "src/components/u1/ExercisesSection.tsx"
      provides: "Tabs 'Ejercicios SQL' y 'Ejercicios Resueltos'"
      contains: "SolvedExercisesSection"
  key_links:
    - from: "src/components/u1/SolvedExerciseCard.tsx"
      to: "src/content/units/u1/solved-exercises.ts"
      via: "SolvedExercise type"
      pattern: "import.*SolvedExercise.*from.*solved-exercises"
    - from: "src/components/u1/SolvedExercisesSection.tsx"
      to: "src/content/units/u1/solved-exercises.ts"
      via: "import U1_SOLVED_EXERCISES"
      pattern: "import.*U1_SOLVED_EXERCISES.*from.*solved-exercises"
    - from: "src/components/u1/ExercisesSection.tsx"
      to: "src/components/u1/SolvedExercisesSection.tsx"
      via: "tab render"
      pattern: "SolvedExercisesSection"
---

<objective>
Ejercicios resueltos con revelacion progresiva para U1 en /unit/u1/ejercicios: 8 ejercicios conceptuales (2 por tipo, 4 tipos) agrupados por categoria, con boton "Ver paso N" para revelacion secuencial, integrados como segunda tab en la seccion Ejercicios existente.

Purpose: Practica guiada — el estudiante piensa antes de ver la solucion, reforzando comprension conceptual mas alla del SQL.
Output: SolvedExercisesSection integrado en ExercisesSection via tabs, 8 ejercicios en solved-exercises.ts.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-autoevaluacion/03-CONTEXT.md
@.planning/phases/03-autoevaluacion/03-RESEARCH.md
@.planning/phases/03-autoevaluacion/03-01-SUMMARY.md
@.planning/phases/03-autoevaluacion/03-02-SUMMARY.md

<interfaces>
<!-- ExercisesSection actual — sera modificado para agregar tabs -->
From src/components/u1/ExercisesSection.tsx:
```typescript
import { useState, useEffect } from 'react'
import { initDb } from '@/engine/sql'
import { U1_EXERCISES } from '@/content/units/u1/exercises'
import { ExerciseCard } from './ExerciseCard'

export default function ExercisesSection() {
	const [completedIds, setCompletedIds] = useState<Set<string>>(new Set())
	useEffect(() => { initDb() }, [])
	// ... renderiza lista de ExerciseCard
}
```

<!-- Patron toggle de ExerciseCard — referencia para reveal progresivo -->
From src/components/u1/ExerciseCard.tsx:
```tsx
<button
	onClick={() => setShowHint(!showHint)}
	className="flex items-center gap-1.5 text-sm text-blue-400 hover:text-blue-300 transition-colors"
>
	<Lightbulb size={14} />
	{showHint ? 'Ocultar pista' : 'Ver pista'}
</button>
```

<!-- Patron tabs de ConceptsSection — referencia para tab switcher -->
From src/components/u1/ConceptsSection.tsx (pattern):
```tsx
// Tabs con aria-selected, cambio de contenido por estado local
<button
	role="tab"
	aria-selected={activeTab === tab.id}
	className={`... ${activeTab === tab.id ? 'text-zinc-100 border-blue-500' : 'text-zinc-400 border-transparent'}`}
	onClick={() => setActiveTab(tab.id)}
>
	{tab.label}
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datos ejercicios resueltos + SolvedExerciseCard + SolvedExercisesSection</name>
  <files>src/content/units/u1/solved-exercises.ts, src/components/u1/SolvedExerciseCard.tsx, src/components/u1/SolvedExercisesSection.tsx</files>
  <action>
**solved-exercises.ts** — Datos estructurados (sin JSX):
- Interface `SolvedStep`: `{ step: number, text: string }`
- Interface `SolvedExercise`: `{ id: string, type: 'desventajas-archivos' | 'clasificar-bd' | 'niveles-organizacionales' | 'problemas-soluciones', title: string, scenario: string, steps: SolvedStep[] }`
- Array `U1_SOLVED_EXERCISES: SolvedExercise[]` con 8 ejercicios (2 por tipo):

  **Tipo 1 - Desventajas archivos** (EJER-01): Escenarios donde el estudiante identifica problemas del enfoque de archivos (ej: "La biblioteca de la UTFSM tiene archivos Excel separados por facultad...", "Spotify guardaba playlists en archivos locales...")

  **Tipo 2 - Clasificar BD** (EJER-02): Escenarios donde clasificar una BD segun criterios (ej: "Netflix necesita almacenar catalogo de peliculas y recomendaciones... Clasifica segun modelo de datos, proposito y contenido")

  **Tipo 3 - Niveles organizacionales** (EJER-03): Escenarios donde mapear nivel organizacional a tipo de SI (ej: "En una clinica: el director revisa metricas mensuales, los medicos consultan fichas, recepcion agenda citas...")

  **Tipo 4 - Problemas vs soluciones** (EJER-04): Escenarios donde relacionar problemas del enfoque archivos con soluciones BD (ej: "Un hospital tiene datos duplicados de pacientes... Que solucion ofrece una BD?")

- Cada ejercicio: 3-5 pasos de revelacion progresiva. Tono coloquial, referencias a UTFSM/empresas tech.
- Sin badge de dificultad (decision locked).

**SolvedExerciseCard.tsx** — Card con revelacion progresiva:
- Props: `{ exercise: SolvedExercise }`
- Estado: `revealedSteps: number` (useState, inicia en 0)
- Renderiza: titulo, escenario visible siempre, pasos revelados (1..revealedSteps), boton "Ver paso N+1" si quedan pasos
- Boton sigue patron visual de ExerciseCard (text-blue-400, Eye icon, hover text-blue-300)
- Pasos revelados con borde izquierdo blue (border-l-2 border-blue-500 pl-4) para distinguir del enunciado
- Cuando todos los pasos estan revelados, mostrar CheckCircle2 verde "Ejercicio completo"

**SolvedExercisesSection.tsx** — Lista agrupada por tipo:
- Agrupa U1_SOLVED_EXERCISES por `type`
- 4 secciones con heading del tipo (ej: "Desventajas del enfoque de archivos", "Clasificar tipos de BD", etc.)
- Cada seccion renderiza sus 2 SolvedExerciseCards
- Estilos dark mode consistentes
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>solved-exercises.ts tiene 8 ejercicios tipados, SolvedExerciseCard implementa reveal progresivo, SolvedExercisesSection agrupa por tipo</done>
</task>

<task type="auto">
  <name>Task 2: Integrar en ExercisesSection con tabs SQL/Resueltos</name>
  <files>src/components/u1/ExercisesSection.tsx</files>
  <action>
Modificar ExercisesSection.tsx para agregar tab switcher entre "Ejercicios SQL" y "Ejercicios Resueltos":

1. Agregar import de SolvedExercisesSection
2. Agregar estado `activeTab: 'sql' | 'resueltos'` (default 'sql')
3. Reemplazar el heading actual por un tab bar con 2 tabs:
   - "Ejercicios SQL" (icono Database o Code)
   - "Ejercicios Resueltos" (icono BookOpen o GraduationCap)
   - Patron visual: seguir el tab pattern de ConceptsSection (border-b blue-500 para activo, zinc-400 para inactivo, aria-selected)
4. Contenido condicional:
   - Tab 'sql': el contenido actual de ExercisesSection (lista de ExerciseCard con initDb, completedIds, etc.)
   - Tab 'resueltos': `<SolvedExercisesSection />`
5. Mantener initDb() en useEffect — solo aplica a tab SQL pero no molesta si se ejecuta siempre
6. NO crear nueva ruta ni nuevo SectionId — todo vive dentro de /unit/u1/ejercicios (decision de RESEARCH.md, evita pitfall "ejercicios route conflict")
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ExercisesSection muestra 2 tabs: SQL exercises (existentes) y Ejercicios Resueltos (nuevos), navegacion por tabs funcional, build sin errores</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` pasa sin errores
- `npm run build` completa sin errores
- /unit/u1/ejercicios muestra tabs SQL y Resueltos
- Tab Resueltos muestra 8 ejercicios agrupados por tipo con revelacion progresiva
</verification>

<success_criteria>
- 8 ejercicios resueltos (2 por tipo, 4 tipos) con escenarios vida real
- Revelacion progresiva paso a paso funcional
- Tabs SQL/Resueltos en ExercisesSection sin romper ejercicios SQL existentes
- Sin nuevas rutas ni SectionIds agregados
- Build pasa sin errores TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-autoevaluacion/03-03-SUMMARY.md`
</output>
