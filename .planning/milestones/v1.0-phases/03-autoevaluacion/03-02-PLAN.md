---
phase: 03-autoevaluacion
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/content/units/u1/cheatsheet.tsx
  - src/components/u1/CheatSheetSection.tsx
  - src/pages/SectionPage.tsx
autonomous: true
requirements: [CHEAT-01, CHEAT-02, CHEAT-03, CHEAT-04, CHEAT-05, CHEAT-06]

must_haves:
  truths:
    - "El estudiante puede consultar definiciones clave (Dato, Informacion, BD, DBMS, DDL, DML, DCL) en el cheat sheet"
    - "El estudiante ve tabla comparativa archivos vs BD, mapa de tipos BD, etapas de diseno, niveles organizacionales y terminologia relacional"
    - "El estudiante navega entre las 6 secciones del cheat sheet con links de ancla sticky"
  artifacts:
    - path: "src/content/units/u1/cheatsheet.tsx"
      provides: "6 secciones de contenido TSX del cheat sheet"
      exports: ["CSDefiniciones", "CSArchivosVsBd", "CSTiposBd", "CSEtapasDiseno", "CSNiveles", "CSTerminologia"]
    - path: "src/components/u1/CheatSheetSection.tsx"
      provides: "Pagina scrollable con sticky nav y las 6 secciones"
      exports: ["default"]
    - path: "src/pages/SectionPage.tsx"
      provides: "Branch condicional u1/cheat-sheet -> CheatSheetSection"
      contains: "CheatSheetSection"
  key_links:
    - from: "src/components/u1/CheatSheetSection.tsx"
      to: "src/content/units/u1/cheatsheet.tsx"
      via: "import CS* functions"
      pattern: "import.*from.*cheatsheet"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/CheatSheetSection.tsx"
      via: "conditional render"
      pattern: "sectionData\\.id === 'cheat-sheet'"
---

<objective>
Cheat sheet visual de referencia para U1 en /unit/u1/cheat-sheet: pagina unica scrollable con 6 secciones (definiciones, archivos vs BD, tipos BD, etapas diseno, niveles organizacionales, terminologia relacional) y barra sticky de navegacion por anclas.

Purpose: Referencia rapida tipo "ultima hora antes del certamen" — el estudiante consulta sin navegar fuera de la plataforma.
Output: CheatSheetSection montado en ruta /unit/u1/cheat-sheet, contenido completo en cheatsheet.tsx.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-autoevaluacion/03-CONTEXT.md
@.planning/phases/03-autoevaluacion/03-RESEARCH.md
@.planning/phases/03-autoevaluacion/03-01-SUMMARY.md

<interfaces>
<!-- Patron de tablas de concepts.tsx — REUTILIZAR en cheat sheet -->
```tsx
<table className="w-full text-sm border-collapse">
	<thead>
		<tr className="bg-zinc-800">
			<th className="text-left px-4 py-3 text-zinc-300 font-semibold border-b border-zinc-700">Columna</th>
		</tr>
	</thead>
	<tbody>
		{rows.map((row, i) => (
			<tr key={row.key} className={i % 2 === 0 ? 'bg-zinc-900' : 'bg-zinc-900/50'}>
				<td className="px-4 py-2.5 text-zinc-200 font-medium border-b border-zinc-800">{row.value}</td>
			</tr>
		))}
	</tbody>
</table>
```

<!-- Sticky nav pattern de RESEARCH.md -->
```tsx
<nav className="sticky top-0 z-10 bg-zinc-950 border-b border-zinc-800 py-2 flex gap-4 overflow-x-auto">
	{SECTIONS.map(s => (
		<a key={s.id} href={`#${s.id}`}
			className="text-sm text-zinc-400 hover:text-zinc-200 whitespace-nowrap px-2 py-1 rounded hover:bg-zinc-800 transition-colors">
			{s.label}
		</a>
	))}
</nav>
```

From src/pages/SectionPage.tsx — imports existentes al momento de ejecutar este plan:
```typescript
import QuizSection from '@/components/u1/QuizSection'  // agregado en 03-01
// Branches existentes: conceptos, editor-sql, ejercicios, quiz
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contenido cheat sheet TSX + CheatSheetSection con sticky nav</name>
  <files>src/content/units/u1/cheatsheet.tsx, src/components/u1/CheatSheetSection.tsx</files>
  <action>
**cheatsheet.tsx** — Contenido TSX, 6 funciones exportadas (patron Topic11/Topic12 de concepts.tsx):

1. **CSDefiniciones** (CHEAT-01): Glosario compacto con 7+ terminos — Dato, Informacion, Base de Datos, DBMS, DDL, DML, DCL. Formato: termino en negrita + definicion corta 1-2 lineas. Usar `<dl>` semantico con `<dt>` bold zinc-100 y `<dd>` zinc-300.

2. **CSArchivosVsBd** (CHEAT-02): Tabla comparativa enfoque archivos vs BD. Reutilizar exactamente el patron de tabla de concepts.tsx (bg-zinc-800 thead, filas alternadas). Columnas: Criterio | Archivos | Base de Datos.

3. **CSTiposBd** (CHEAT-03): Mapa de tipos de BD segun los 6 criterios de clasificacion del temario. Tabla con columnas: Criterio | Tipos | Ejemplo breve. 6 filas (Modelo datos, Contenido, Usuarios, Sitio, Funcion, Proposito).

4. **CSEtapasDiseno** (CHEAT-04): Las 6 etapas del proceso de diseno de BD con objetivo de cada una. Formato cards o lista numerada con nombre etapa en bold + objetivo en 1 linea.

5. **CSNiveles** (CHEAT-05): Niveles organizacionales (estrategico, tactico, operativo) mapeados a tipos de SI (DSS/MIS/TPS). Tabla o diagrama visual con 3 filas.

6. **CSTerminologia** (CHEAT-06): Terminologia relacional — relacion/tupla/atributo/dominio/clave con equivalencias informales (tabla/fila/columna). Formato glosario o tabla de 2 columnas (Termino formal | Equivalente).

**Contenido fiel al temario INF-239** — consultar `src/content/units/u1/concepts.tsx` para datos exactos. Es un RESUMEN, no repeticion completa. Compacto, escaneable, de referencia rapida.

**CheatSheetSection.tsx** — Componente wrapper:
- Array `SECTIONS` con `{ id, label }` para las 6 secciones (ids: 'definiciones', 'archivos-vs-bd', 'tipos-bd', 'etapas-diseno', 'niveles', 'terminologia')
- Barra `<nav>` sticky con links de ancla (ver RESEARCH.md Pattern 4). `scroll-behavior: smooth` via clase `scroll-smooth` en el contenedor o verificar si ya existe en index.css
- Cada seccion tiene `<section id="xxx">` con heading `<h2>` + contenido importado de cheatsheet.tsx
- Estilos dark mode consistentes: zinc-900 bg, zinc-100/300 text, blue-500 accents
- PITFALL sticky nav: si top-0 queda detras del header, usar top offset segun altura del header de AppLayout (verificar AppLayout.tsx)
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>cheatsheet.tsx tiene 6 secciones exportadas con contenido fiel al temario, CheatSheetSection renderiza sticky nav + 6 secciones scrollables</done>
</task>

<task type="auto">
  <name>Task 2: Wiring CheatSheetSection en SectionPage</name>
  <files>src/pages/SectionPage.tsx</files>
  <action>
En SectionPage.tsx:
1. Agregar import: `import CheatSheetSection from '@/components/u1/CheatSheetSection'`
2. Agregar branch condicional ANTES del return final con PlaceholderSection (despues del branch quiz agregado en 03-01):
```tsx
if (unit.id === 'u1' && sectionData.id === 'cheat-sheet') {
	return (
		<div>
			{breadcrumb}
			<CheatSheetSection />
		</div>
	)
}
```
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Ruta /unit/u1/cheat-sheet renderiza CheatSheetSection en vez de PlaceholderSection, build pasa sin errores</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` pasa sin errores
- `npm run build` completa sin errores
- Cheat sheet visible en /unit/u1/cheat-sheet con 6 secciones y sticky nav
</verification>

<success_criteria>
- 6 secciones del cheat sheet con contenido fiel al temario INF-239
- Sticky nav con links de ancla funcionales (scroll suave)
- Tablas reutilizan estilo de concepts.tsx (filas alternadas, dark mode)
- Build pasa sin errores TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-autoevaluacion/03-02-SUMMARY.md`
</output>
