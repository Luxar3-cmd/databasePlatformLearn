---
phase: 02-contenido-u1-y-editor-sql
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/hooks/useSqlEngine.ts
  - src/components/u1/SqlEditor.tsx
  - src/components/u1/ResultsTable.tsx
  - src/components/u1/SchemaViewer.tsx
  - src/components/u1/EditorSection.tsx
  - src/pages/SectionPage.tsx
autonomous: true
requirements: [SQL-02, SQL-03, SQL-04, SQL-05, SQL-06, SQL-07, SQL-09]

must_haves:
  truths:
    - "El estudiante escribe SQL en un editor con syntax highlighting y ejecuta con Ctrl+Enter o boton Ejecutar"
    - "Los resultados aparecen en una tabla con columnas y filas debajo del editor"
    - "Si el SQL es invalido, aparece un mensaje de error en espanol en rojo"
    - "El estudiante puede ver el esquema de la BD expandiendo un acordeon arriba del editor"
    - "El estudiante puede resetear la BD al estado original con un boton"
    - "Botones de consultas rapidas insertan y ejecutan SELECT * FROM tabla automaticamente"
  artifacts:
    - path: "src/hooks/useSqlEngine.ts"
      provides: "Hook React que encapsula estado SQL (result, loading) y llama engine/sql.ts"
      exports: ["useSqlEngine"]
    - path: "src/components/u1/SqlEditor.tsx"
      provides: "CodeMirror 6 wrapper con keymap Ctrl+Enter y tema oscuro"
      exports: ["SqlEditor"]
    - path: "src/components/u1/ResultsTable.tsx"
      provides: "Tabla dinamica de resultados SQL"
      exports: ["ResultsTable"]
    - path: "src/components/u1/SchemaViewer.tsx"
      provides: "Acordeon expandible con schema de las 5 tablas"
      exports: ["SchemaViewer"]
    - path: "src/components/u1/EditorSection.tsx"
      provides: "Layout completo: schema viewer + editor + results + botones"
      exports: ["default (EditorSection)"]
  key_links:
    - from: "src/components/u1/EditorSection.tsx"
      to: "src/hooks/useSqlEngine.ts"
      via: "useSqlEngine hook"
      pattern: "useSqlEngine\\(\\)"
    - from: "src/hooks/useSqlEngine.ts"
      to: "src/engine/sql.ts"
      via: "import functions"
      pattern: "import.*from.*engine/sql"
    - from: "src/components/u1/SqlEditor.tsx"
      to: "@uiw/react-codemirror"
      via: "CodeMirror component"
      pattern: "import CodeMirror from"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/EditorSection.tsx"
      via: "conditional render por sectionId"
      pattern: "editor-sql.*EditorSection"
---

<objective>
Construir el editor SQL interactivo completo: hook useSqlEngine, editor CodeMirror con Ctrl+Enter, tabla de resultados dinamica, visor de esquema en acordeon, botones de consultas rapidas, boton reset. Wiring en SectionPage para la seccion editor-sql.

Purpose: Este es el diferenciador tecnico de la plataforma — el estudiante ejecuta SQL real en el browser.
Output: EditorSection montado en /unit/u1/editor-sql, completamente funcional.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-CONTEXT.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-RESEARCH.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-01-SUMMARY.md

@src/engine/sql.ts
@src/engine/schema.ts
@src/types/sql.ts
@src/pages/SectionPage.tsx

<interfaces>
<!-- Contracts from plan 02-01 that this plan consumes -->

From src/types/sql.ts:
```typescript
export type QueryResult =
  | { ok: true; rows: Record<string, unknown>[]; rowCount: number }
  | { ok: false; error: string }

export interface TableSchema {
  nombre: string
  columnas: ColumnDef[]
}
```

From src/engine/sql.ts:
```typescript
export function initDb(): void
export function resetDb(): void
export function executeQuery(sql: string): QueryResult
```

From src/engine/schema.ts:
```typescript
export const DB_SCHEMA: TableSchema[]
export const TABLE_NAMES: string[]
```

From src/pages/SectionPage.tsx (current state):
```typescript
// Renderiza PlaceholderSection para todas las secciones
// Necesita agregar branch condicional para 'editor-sql' -> <EditorSection />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSqlEngine hook + SqlEditor + ResultsTable + SchemaViewer</name>
  <files>src/hooks/useSqlEngine.ts, src/components/u1/SqlEditor.tsx, src/components/u1/ResultsTable.tsx, src/components/u1/SchemaViewer.tsx</files>
  <action>
1. Crear `src/hooks/useSqlEngine.ts`:
   - Estado: `{ result: QueryResult | null, isLoading: boolean }`
   - `useEffect(() => { initDb() }, [])` — llama initDb una sola vez (engine tiene guard interno)
   - `execute(sql: string)`: setState loading, llama executeQuery, setState result
   - `reset()`: llama resetDb(), setState result null
   - Retorna `{ result, isLoading, execute, reset }`

2. Crear `src/components/u1/SqlEditor.tsx`:
   - Props: `{ value: string, onChange: (val: string) => void, onExecute: () => void }`
   - Importar `CodeMirror` de `@uiw/react-codemirror`, `sql` de `@codemirror/lang-sql`, `keymap` de `@codemirror/view`, `okaidia` de `@uiw/codemirror-theme-okaidia`
   - Keymap custom PRIMERO en extensions array: `keymap.of([{ key: 'Mod-Enter', run: () => { onExecute(); return true } }])`
   - Extensions: `[executeKeymap, sql()]`
   - Theme: okaidia (dark mode)
   - Height: "200px"
   - Clases Tailwind para border/rounding que combine con zinc palette

3. Crear `src/components/u1/ResultsTable.tsx`:
   - Props: `{ result: QueryResult | null }`
   - Si result es null: no renderizar nada
   - Si result.ok === false: mostrar error en rojo con icono AlertCircle de lucide, texto en espanol
   - Si result.ok === true y rows.length === 0: mostrar "Consulta ejecutada. X filas afectadas." (para DML) o "Sin resultados." (para SELECT vacio)
   - Si result.ok === true y rows.length > 0: tabla HTML con Object.keys(rows[0]) como headers, filas alternadas (zinc-800/zinc-900), bordes sutiles zinc-700, overflow-x-auto para tablas anchas
   - Usar font-mono (JetBrains Mono ya cargada) para valores de la tabla

4. Crear `src/components/u1/SchemaViewer.tsx`:
   - Props: ninguno (importa DB_SCHEMA directamente de engine/schema)
   - Estado: `expanded: boolean` (default false)
   - Boton toggle "Esquema de la BD" con icono ChevronDown/ChevronUp de lucide
   - Cuando expandido: grid de cards (una por tabla), cada card muestra nombre de tabla como titulo y lista de columnas con tipo y restriccion
   - Usar Tailwind: bg-zinc-800, border-zinc-700, text-sm
   - Acordeon con transicion suave (max-height + overflow-hidden)
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Hook useSqlEngine funcional, SqlEditor con CodeMirror + Ctrl+Enter, ResultsTable renderiza resultados/errores, SchemaViewer muestra schema en acordeon. Todos type-check sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: EditorSection layout completo + wiring en SectionPage</name>
  <files>src/components/u1/EditorSection.tsx, src/pages/SectionPage.tsx</files>
  <action>
1. Crear `src/components/u1/EditorSection.tsx`:
   - Layout apilado (decision locked en CONTEXT.md): SchemaViewer arriba, editor en el medio, resultados debajo
   - Usa useSqlEngine hook para estado y ejecucion
   - Estado local: `query: string` (default `'SELECT * FROM alumnos;'`)
   - Estructura del componente:
     a. SchemaViewer (acordeon expandible)
     b. Barra de acciones superior:
        - Botones de consultas rapidas: un boton por cada tabla de TABLE_NAMES que al hacer click setea query a `SELECT * FROM {tabla};` y ejecuta inmediatamente (decision locked: "inserta y ejecuta automaticamente")
        - Boton "Ejecutar" (Play icon) — llama execute(query)
        - Boton "Reset BD" (RefreshCw icon) — llama reset(), muestra confirmacion breve
        - Hint visual "Ctrl+Enter para ejecutar" en texto zinc-500 pequeno
     c. SqlEditor con value={query}, onChange, onExecute
     d. ResultsTable con result del hook
   - Espaciado consistente con gap-4 entre secciones
   - Los botones de consulta rapida deben tener estilo minimal: bg-zinc-800 hover:bg-zinc-700, texto sm

2. Modificar `src/pages/SectionPage.tsx`:
   - Importar EditorSection (lazy o directo — directo esta bien por ahora, el componente no es pesado para SSR ya que no hay SSR)
   - En el render, antes del PlaceholderSection fallback, agregar:
     ```tsx
     if (unit.id === 'u1' && sectionData.id === 'editor-sql') {
       return <div>...breadcrumb existente...<EditorSection /></div>
     }
     ```
   - Mantener el breadcrumb existente (Unidad X > Seccion) arriba del componente real
   - El PlaceholderSection sigue siendo el fallback para secciones no implementadas

3. Verificar que `npm run build` pasa y que la ruta `/unit/u1/editor-sql` renderiza el editor.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>EditorSection montado en /unit/u1/editor-sql. Layout apilado: schema viewer expandible, botones de consultas rapidas por tabla, editor CodeMirror con Ctrl+Enter, tabla de resultados, errores en espanol, boton reset. Build pasa.</done>
</task>

</tasks>

<verification>
- `npm run build` pasa sin errores
- Ruta /unit/u1/editor-sql muestra EditorSection (no PlaceholderSection)
- CodeMirror renderiza con syntax highlighting SQL y tema oscuro
- Ctrl+Enter ejecuta la query
- Boton Ejecutar ejecuta la query
- Tabla de resultados muestra columnas y filas
- Error SQL muestra mensaje en espanol en rojo
- SchemaViewer muestra las 5 tablas al expandir
- Botones de consultas rapidas insertan y ejecutan SELECT * FROM tabla
- Boton Reset restaura BD
</verification>

<success_criteria>
- Editor SQL completamente funcional en /unit/u1/editor-sql
- 7 requirements cubiertos: SQL-02 (CodeMirror), SQL-03 (Ctrl+Enter + boton), SQL-04 (tabla resultados), SQL-05 (errores espanol), SQL-06 (reset), SQL-07 (visor esquema), SQL-09 (consultas rapidas)
</success_criteria>

<output>
After completion, create `.planning/phases/02-contenido-u1-y-editor-sql/02-02-SUMMARY.md`
</output>
