---
phase: 02-contenido-u1-y-editor-sql
plan: 05
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/pages/SectionPage.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "El estudiante puede navegar entre Conceptos, Editor SQL y Ejercicios desde el sidebar y todas renderizan correctamente"
    - "El estudiante puede leer las 4 secciones teoricas con tabs"
    - "El estudiante puede ejecutar SQL real en el editor con resultados visibles"
    - "El estudiante puede practicar con los 6 ejercicios guiados"
    - "Las secciones Quiz y Cheat Sheet siguen mostrando placeholder"
  artifacts:
    - path: "src/pages/SectionPage.tsx"
      provides: "Router condicional para las 3 secciones implementadas de U1"
  key_links:
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/ConceptsSection.tsx"
      via: "import + conditional render"
      pattern: "conceptos.*ConceptsSection"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/EditorSection.tsx"
      via: "import + conditional render"
      pattern: "editor-sql.*EditorSection"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/ExercisesSection.tsx"
      via: "import + conditional render"
      pattern: "ejercicios.*ExercisesSection"
---

<objective>
Verificacion final de la fase: asegurar que SectionPage tiene los 3 branches correctos (conceptos, editor-sql, ejercicios), todo funciona end-to-end, y obtener aprobacion visual del usuario.

Purpose: Garantizar integracion correcta de los 3 subsistemas antes de cerrar la fase.
Output: Fase 2 verificada y aprobada.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-CONTEXT.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-01-SUMMARY.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-02-SUMMARY.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-03-SUMMARY.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-04-SUMMARY.md

@src/pages/SectionPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar integracion SectionPage y corregir issues</name>
  <files>src/pages/SectionPage.tsx</files>
  <action>
1. Leer `src/pages/SectionPage.tsx` actual y verificar que tiene los 3 branches correctos:
   - `unit.id === 'u1' && sectionData.id === 'conceptos'` -> ConceptsSection
   - `unit.id === 'u1' && sectionData.id === 'editor-sql'` -> EditorSection
   - `unit.id === 'u1' && sectionData.id === 'ejercicios'` -> ExercisesSection
   - Fallback: PlaceholderSection (para quiz, cheat-sheet, y secciones de U2-U6)

2. Si los branches fueron agregados por plans separados y el codigo esta inconsistente (imports duplicados, estructura irregular), refactorizar a una funcion `renderSection` limpia:
   ```tsx
   function renderSection(sectionId: SectionId, unitId: string) {
     if (unitId === 'u1') {
       switch (sectionId) {
         case 'conceptos': return <ConceptsSection />
         case 'editor-sql': return <EditorSection />
         case 'ejercicios': return <ExercisesSection />
       }
     }
     return <PlaceholderSection ... />
   }
   ```

3. Verificar build final: `npm run build` debe pasar sin errores ni warnings.

4. Iniciar dev server y verificar que las 5 rutas de U1 cargan:
   - /unit/u1/conceptos -> tabs con contenido
   - /unit/u1/editor-sql -> editor SQL funcional
   - /unit/u1/ejercicios -> 6 exercise cards
   - /unit/u1/quiz -> PlaceholderSection
   - /unit/u1/cheat-sheet -> PlaceholderSection
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>SectionPage integra correctamente los 3 componentes reales. Build pasa. Rutas cargan sin errores.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacion visual completa de Fase 2</name>
  <files>src/pages/SectionPage.tsx</files>
  <action>
Verificacion humana de toda la fase. El usuario debe probar los 3 subsistemas implementados:
- Conceptos: 4 tabs con contenido teorico, callout boxes, tablas comparativas
- Editor SQL: CodeMirror con Ctrl+Enter, tabla de resultados, schema viewer, reset, consultas rapidas, errores en espanol
- Ejercicios: 6 cards con mini-editor, pistas, soluciones, validacion automatica

Pasos de verificacion:
1. `npm run dev` (ya deberia estar corriendo)
2. http://localhost:5173/unit/u1/conceptos — 4 tabs, contenido fiel al curso, callouts
3. http://localhost:5173/unit/u1/editor-sql — ejecutar SQL, schema, reset, errores
4. http://localhost:5173/unit/u1/ejercicios — 6 ejercicios, pistas, validacion
5. /unit/u1/quiz y /unit/u1/cheat-sheet — placeholder
6. Responsive en mobile
  </action>
  <verify>Usuario aprueba la verificacion visual escribiendo "approved"</verify>
  <done>Fase 2 completa y aprobada por el usuario. Los 5 success criteria del ROADMAP validados.</done>
</task>

</tasks>

<verification>
- Build pasa sin errores
- Las 3 secciones implementadas renderizan correctamente
- Las 2 secciones pendientes (quiz, cheat-sheet) muestran placeholder
- Usuario aprueba la verificacion visual
</verification>

<success_criteria>
- Phase 2 success criteria from ROADMAP validated:
  1. Estudiante puede leer secciones 1.1-1.4 con definiciones, tablas comparativas, listados
  2. Estudiante escribe SELECT, presiona Ctrl+Enter, ve resultados en tabla
  3. SQL invalido muestra error claro en espanol
  4. Estudiante ve esquema de BD y puede resetear
  5. Estudiante puede seguir 6 ejercicios guiados con hints y solucion
</success_criteria>

<output>
After completion, create `.planning/phases/02-contenido-u1-y-editor-sql/02-05-SUMMARY.md`
</output>
