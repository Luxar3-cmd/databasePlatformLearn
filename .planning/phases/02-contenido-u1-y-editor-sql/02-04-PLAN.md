---
phase: 02-contenido-u1-y-editor-sql
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/content/units/u1/exercises.ts
  - src/components/u1/ExerciseCard.tsx
  - src/components/u1/ExercisesSection.tsx
  - src/pages/SectionPage.tsx
autonomous: true
requirements: [SQL-08]

must_haves:
  truths:
    - "El estudiante ve 6 ejercicios como cards seleccionables con dificultad progresiva"
    - "Cada ejercicio tiene su propio mini-editor SQL embebido"
    - "El estudiante puede pedir una pista y ver la solucion completa por separado"
    - "La validacion compara resultados del estudiante vs la solucion automaticamente"
    - "El estudiante ve cuales ejercicios ha completado"
  artifacts:
    - path: "src/content/units/u1/exercises.ts"
      provides: "Array de 6 ejercicios tipados con hints y soluciones"
      exports: ["U1_EXERCISES"]
    - path: "src/components/u1/ExerciseCard.tsx"
      provides: "Card individual con mini-editor, pista, solucion, validacion"
      exports: ["ExerciseCard"]
    - path: "src/components/u1/ExercisesSection.tsx"
      provides: "Lista de exercise cards con estado de completado"
      exports: ["default (ExercisesSection)"]
  key_links:
    - from: "src/components/u1/ExerciseCard.tsx"
      to: "src/components/u1/SqlEditor.tsx"
      via: "reutiliza SqlEditor de plan 02-02"
      pattern: "import.*SqlEditor"
    - from: "src/components/u1/ExerciseCard.tsx"
      to: "src/engine/sql.ts"
      via: "executeQuery para validacion"
      pattern: "import.*executeQuery"
    - from: "src/components/u1/ExercisesSection.tsx"
      to: "src/content/units/u1/exercises.ts"
      via: "import exercise data"
      pattern: "import.*U1_EXERCISES"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/u1/ExercisesSection.tsx"
      via: "conditional render por sectionId"
      pattern: "ejercicios.*ExercisesSection"
---

<objective>
Implementar los 6 ejercicios guiados SQL con mini-editor por ejercicio, pistas, soluciones, y validacion automatica de resultados. Wiring en SectionPage para la seccion ejercicios.

Purpose: Los ejercicios guiados son la practica progresiva que conecta la teoria con la ejecucion SQL real.
Output: ExercisesSection montado en /unit/u1/ejercicios con 6 ejercicios funcionales.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-CONTEXT.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-RESEARCH.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-01-SUMMARY.md
@.planning/phases/02-contenido-u1-y-editor-sql/02-02-SUMMARY.md

@src/engine/sql.ts
@src/types/sql.ts
@src/components/u1/SqlEditor.tsx
@src/pages/SectionPage.tsx

<interfaces>
<!-- Contracts from previous plans -->

From src/types/sql.ts:
```typescript
export interface Exercise {
  id: string
  title: string
  description: string
  hint: string
  solution: string
  initialQuery?: string
  difficulty: 'basico' | 'intermedio' | 'avanzado'
}
```

From src/engine/sql.ts:
```typescript
export function executeQuery(sql: string): QueryResult
export function initDb(): void
```

From src/components/u1/SqlEditor.tsx:
```typescript
interface SqlEditorProps {
  value: string
  onChange: (val: string) => void
  onExecute: () => void
}
export function SqlEditor(props: SqlEditorProps): JSX.Element
```

From src/components/u1/ResultsTable.tsx:
```typescript
export function ResultsTable({ result }: { result: QueryResult | null }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Datos de ejercicios + ExerciseCard con mini-editor y validacion</name>
  <files>src/content/units/u1/exercises.ts, src/components/u1/ExerciseCard.tsx</files>
  <action>
1. Crear `src/content/units/u1/exercises.ts` — 6 ejercicios progresivos sobre la BD universitaria:

   Ejercicio 1 (basico): "Explorar alumnos"
   - description: "Obtener todos los alumnos ordenados por apellido"
   - hint: "Usa SELECT * con ORDER BY sobre la columna apellido"
   - solution: "SELECT * FROM alumnos ORDER BY apellido"
   - initialQuery: "SELECT * FROM alumnos"

   Ejercicio 2 (basico): "Filtrar por nota"
   - description: "Obtener nombre y apellido de los alumnos con nota mayor o igual a 5.0 en alguna inscripcion"
   - hint: "Necesitas un JOIN entre alumnos e inscripciones, y un WHERE sobre la nota"
   - solution: "SELECT DISTINCT a.nombre, a.apellido FROM alumnos a JOIN inscripciones i ON a.id = i.alumno_id WHERE i.nota >= 5.0"
   - initialQuery: "SELECT * FROM inscripciones WHERE nota >= 5.0"

   Ejercicio 3 (intermedio): "Asignaturas por departamento"
   - description: "Listar las asignaturas del departamento de Informatica con su codigo y creditos"
   - hint: "JOIN entre asignaturas y departamentos, filtrando por nombre del departamento"
   - solution: "SELECT asig.codigo, asig.nombre, asig.creditos FROM asignaturas asig JOIN departamentos d ON asig.departamento_id = d.id WHERE d.nombre = 'Informatica'"

   Ejercicio 4 (intermedio): "Conteo de inscripciones"
   - description: "Contar cuantos alumnos estan inscritos en cada asignatura, mostrando el codigo de la asignatura"
   - hint: "Usa GROUP BY sobre el codigo de asignatura con COUNT"
   - solution: "SELECT asig.codigo, COUNT(*) as total_inscritos FROM inscripciones i JOIN asignaturas asig ON i.asignatura_id = asig.id GROUP BY asig.codigo"

   Ejercicio 5 (avanzado): "Promedio por asignatura"
   - description: "Calcular el promedio de notas por asignatura, mostrando solo las que tengan promedio sobre 4.0"
   - hint: "Usa AVG con GROUP BY y filtra con HAVING (no WHERE, porque filtras sobre el agregado)"
   - solution: "SELECT asig.codigo, AVG(i.nota) as promedio FROM inscripciones i JOIN asignaturas asig ON i.asignatura_id = asig.id GROUP BY asig.codigo HAVING AVG(i.nota) > 4.0"

   Ejercicio 6 (avanzado): "Profesores y sus asignaturas"
   - description: "Listar todos los profesores con el nombre de su departamento, ordenados por departamento y apellido"
   - hint: "JOIN entre profesores y departamentos, con ORDER BY en dos columnas"
   - solution: "SELECT p.nombre, p.apellido, d.nombre as departamento FROM profesores p JOIN departamentos d ON p.departamento_id = d.id ORDER BY d.nombre, p.apellido"

2. Crear `src/components/u1/ExerciseCard.tsx`:
   - Props: `{ exercise: Exercise, isCompleted: boolean, onComplete: (id: string) => void }`
   - Estado local: `query` (string, default exercise.initialQuery o ''), `result` (QueryResult | null), `showHint` (boolean), `showSolution` (boolean), `validationStatus` ('idle' | 'correct' | 'incorrect')
   - Layout (decision locked: "cada ejercicio tiene su propio editor SQL embebido, autocontenido"):
     a. Header: titulo + badge de dificultad (basico=green, intermedio=amber, avanzado=red) + icono check si completado
     b. Descripcion del ejercicio
     c. Boton "Pista" (decision locked: boton separado) — toggle showHint, muestra hint en callout amarillo
     d. Mini SqlEditor (reutilizar SqlEditor.tsx de 02-02) con height="150px"
     e. Boton "Ejecutar y Validar" — ejecuta query del estudiante Y la solucion, compara resultados
     f. ResultsTable con resultado del estudiante
     g. Feedback de validacion: si correcto, banner verde "Correcto! Tu consulta produce el resultado esperado." + llamar onComplete. Si incorrecto, banner rojo "Resultado diferente al esperado. Intenta de nuevo."
     h. Boton "Ver solucion" (decision locked: boton separado de pista) — toggle showSolution, muestra query completa en bloque code

   - Funcion de validacion (Pattern 5 de RESEARCH.md):
     ```typescript
     function rowsMatch(studentRows, solutionRows): boolean {
       if (studentRows.length !== solutionRows.length) return false
       const normalize = (rows) => rows.map(r => JSON.stringify(r, Object.keys(r).sort())).sort()
       return normalize(studentRows).join() === normalize(solutionRows).join()
     }
     ```
   - Al validar: ejecutar executeQuery con la query del estudiante y con la solucion, comparar con rowsMatch
   - Asegurar que initDb() se llama antes de validar (el useSqlEngine del EditorSection ya lo hace, pero si el usuario navega directo a ejercicios, necesita inicializarse — llamar initDb() en ExercisesSection useEffect)
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>6 ejercicios definidos con dificultad progresiva. ExerciseCard con mini-editor, pista, solucion, validacion automatica por comparacion de resultados. Type-check pasa.</done>
</task>

<task type="auto">
  <name>Task 2: ExercisesSection + wiring en SectionPage</name>
  <files>src/components/u1/ExercisesSection.tsx, src/pages/SectionPage.tsx</files>
  <action>
1. Crear `src/components/u1/ExercisesSection.tsx`:
   - Estado: `completedIds: Set<string>` (en memoria, no persiste — persistencia es v2)
   - useEffect: llamar initDb() para asegurar BD inicializada
   - Layout (decision locked: "cards con los 6 ejercicios, el estudiante elige libremente, ve cuales completo"):
     a. Titulo "Ejercicios Guiados SQL" con subtitulo "Practica con la base de datos universitaria"
     b. Contador: "X de 6 completados"
     c. Lista de ExerciseCard, uno por ejercicio de U1_EXERCISES
     d. Cada card visible completa (no colapsada) — el estudiante hace scroll para recorrer
     e. onComplete: agrega id al Set de completedIds
   - Espaciado entre cards: gap-6

2. Modificar `src/pages/SectionPage.tsx`:
   - Importar ExercisesSection
   - Agregar branch:
     ```tsx
     if (unit.id === 'u1' && sectionData.id === 'ejercicios') {
       return <div>...breadcrumb...<ExercisesSection /></div>
     }
     ```
   - Mantener branches existentes de conceptos y editor-sql

3. Verificar build pasa.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ExercisesSection montado en /unit/u1/ejercicios. 6 ejercicios con mini-editor, pistas, soluciones, validacion automatica. Contador de completados. Build pasa.</done>
</task>

</tasks>

<verification>
- `npm run build` pasa sin errores
- Ruta /unit/u1/ejercicios muestra ExercisesSection (no PlaceholderSection)
- 6 exercise cards visibles con dificultad progresiva
- Cada card tiene mini-editor SQL embebido
- Boton Pista muestra hint
- Boton Ver solucion muestra query completa
- Validacion automatica: correcto marca ejercicio como completado, incorrecto muestra feedback
</verification>

<success_criteria>
- SQL-08 cubierto: 6 ejercicios guiados con hints, soluciones, y validacion automatica
- Mini-editor por ejercicio con CodeMirror
- Comparacion normalizada de resultados (orden de filas y columnas no importa)
</success_criteria>

<output>
After completion, create `.planning/phases/02-contenido-u1-y-editor-sql/02-04-SUMMARY.md`
</output>
