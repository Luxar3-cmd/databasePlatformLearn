---
phase: 04-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - README.md
autonomous: false
requirements:
  - NFR-04
user_setup:
  - service: vercel
    why: "Deploy de la plataforma estatica"
    env_vars: []
    dashboard_config:
      - task: "Login con cuenta Vercel"
        location: "vercel login (CLI interactivo)"

must_haves:
  truths:
    - "La plataforma es accesible desde https://db-platform-u1.vercel.app sin instalar nada"
    - "Hacer refresh en cualquier ruta (/, /unit/u1/concepts, /unit/u1/quiz, /unit/u1/cheatsheet, /unit/u1/exercises) no devuelve 404"
    - "El build local completa sin errores TypeScript antes del deploy"
    - "El README documenta la URL de produccion y el proceso de deploy manual"
  artifacts:
    - path: "vercel.json"
      provides: "SPA rewrite config — todas las rutas devuelven index.html"
      contains: "rewrites"
    - path: "README.md"
      provides: "Documentacion del proyecto con URL de produccion y proceso de deploy"
      contains: "db-platform-u1.vercel.app"
  key_links:
    - from: "vercel.json"
      to: "index.html"
      via: "rewrites /(.*) -> /index.html"
      pattern: "rewrites.*index\\.html"
    - from: "npm run build"
      to: "dist/"
      via: "tsc -b && vite build"
      pattern: "build.*dist"
---

<objective>
Configurar y ejecutar el deploy estatico de la plataforma a Vercel con SPA routing funcional en produccion.

Purpose: Cerrar el ultimo requisito de v1 (NFR-04) — la plataforma debe ser accesible publicamente sin instalar nada. Todas las rutas de React Router deben funcionar al hacer refresh directo (sin el rewrite, Vercel devuelve 404 en cualquier ruta distinta de /).

Output:
- vercel.json en la raiz del proyecto con rewrite SPA
- README.md reemplazado (actualmente contiene el template default de Vite — completamente irrelevante)
- Deploy activo en https://db-platform-u1.vercel.app
- 5 rutas verificadas sin 404 en refresh directo
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear vercel.json y reemplazar README.md</name>
  <files>vercel.json, README.md</files>
  <action>
    Crear vercel.json en la raiz del proyecto con el rewrite SPA para React Router. El archivo debe contener exactamente:

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "rewrites": [
        {
          "source": "/(.*)",
          "destination": "/index.html"
        }
      ]
    }
    ```

    NO agregar buildCommand ni outputDirectory — Vercel detecta Vite automaticamente y usa npm run build + dist/ por default.
    NO agregar framework ni otras propiedades — el archivo debe ser minimo.

    Reemplazar README.md completo (el actual tiene el template default de create-vite, completamente irrelevante). El README nuevo debe contener:
    - Titulo: "BDD Lab UTFSM"
    - Descripcion breve: herramienta de estudio interactivo para INF-239, practica SQL y autoevaluacion en el browser sin instalar nada
    - Seccion "Ejecutar en local": `npm install` + `npm run dev`
    - Seccion "Deploy": URL de produccion (https://db-platform-u1.vercel.app), prerequisito (npm install -g vercel + vercel login), comando de deploy (`vercel --prod`)
    - Seccion "Verificacion post-deploy": checklist con las 5 rutas a probar con refresh directo

    Tambien ejecutar `npm run build` para confirmar que el build local pasa sin errores TypeScript. Si hay errores, corregirlos antes de continuar.
  </action>
  <verify>
    <automated>
      cat /home/imorales/dev/databasePlatformLearn/vercel.json | grep -q "rewrites" && echo "vercel.json OK" || echo "FAIL: vercel.json missing rewrites"
      npm run build --prefix /home/imorales/dev/databasePlatformLearn 2>&1 | tail -5
    </automated>
  </verify>
  <done>vercel.json existe con el rewrite /(.*) -> /index.html. README.md reemplazado con informacion del proyecto. npm run build completa con codigo de salida 0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy a Vercel (interactivo — requiere accion manual)</name>
  <files></files>
  <action>Paso manual requerido: el primer deploy via Vercel CLI es interactivo y requiere login + wizard de configuracion del proyecto. Claude no puede autenticar en nombre del usuario.</action>
  <verify>URL de produccion https://db-platform-u1.vercel.app responde</verify>
  <done>Deploy completado y URL de produccion activa</done>
  <what-built>vercel.json con SPA rewrite listo en el repo. Build local verificado sin errores.</what-built>
  <how-to-verify>
    Ejecutar los siguientes pasos desde la raiz del proyecto:

    1. Instalar Vercel CLI si no esta instalado:
       ```bash
       npm install -g vercel
       ```

    2. Iniciar sesion (si no estas logueado):
       ```bash
       vercel login
       ```

    3. Hacer el deploy a produccion (primer deploy — el wizard es interactivo):
       ```bash
       vercel --prod
       ```

       Cuando el wizard pregunte:
       - "Set up and deploy?" → Y
       - "Which scope?" → tu cuenta personal
       - "Link to existing project?" → N
       - "Project name?" → db-platform-u1
       - "In which directory is your code located?" → ./

       Vercel detecta Vite automaticamente. El deploy deberia completar en ~1 minuto.

    4. Confirmar que la URL en el output es: https://db-platform-u1.vercel.app
       (Si la URL contiene un hash aleatorio tipo db-platform-u1-abc123.vercel.app, usaste `vercel` sin `--prod`. Repetir con `--prod`.)
  </how-to-verify>
  <resume-signal>Escribe "deployed" cuando el deploy complete y la URL de produccion sea https://db-platform-u1.vercel.app (o la URL real que aparecio en el output si el nombre no estaba disponible)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion visual de 5 rutas en produccion</name>
  <files></files>
  <action>Verificacion manual requerida: confirmar que React Router funciona correctamente en produccion para todas las rutas de U1.</action>
  <verify>Las 5 rutas cargan sin 404 al hacer refresh directo en el browser</verify>
  <done>NFR-04 cumplido: plataforma publica, todas las rutas funcionan en produccion</done>
  <what-built>Deploy en produccion con vercel.json configurado para SPA routing.</what-built>
  <how-to-verify>
    Verificar que las siguientes rutas NO devuelven 404 al hacer refresh directo en el browser (Ctrl+R o F5 en cada una):

    1. https://db-platform-u1.vercel.app/
       Esperado: Pagina principal de la plataforma con sidebar

    2. https://db-platform-u1.vercel.app/unit/u1/concepts
       Esperado: Seccion Conceptos de U1

    3. https://db-platform-u1.vercel.app/unit/u1/quiz
       Esperado: Quiz de U1

    4. https://db-platform-u1.vercel.app/unit/u1/cheatsheet
       Esperado: Cheat sheet de U1

    5. https://db-platform-u1.vercel.app/unit/u1/exercises
       Esperado: Seccion Ejercicios de U1

    Si alguna ruta devuelve "404: NOT_FOUND" de Vercel (no el 404 de React Router), el vercel.json no se aplico correctamente. Verificar que vercel.json esta en la raiz del repo y hacer `vercel --prod` nuevamente.
  </how-to-verify>
  <resume-signal>Escribe "verified" si las 5 rutas cargan sin 404. Describe cualquier ruta que falle.</resume-signal>
</task>

</tasks>

<verification>
Post-deploy checks:
- vercel.json en la raiz del repo con rewrite /(.*) -> /index.html
- README.md contiene URL de produccion y proceso de deploy
- Las 5 rutas criticas responden sin 404 en refresh directo
- NFR-04 marcado como complete en REQUIREMENTS.md
</verification>

<success_criteria>
- La plataforma es accesible en https://db-platform-u1.vercel.app (o URL real del deploy)
- Refresh directo en /unit/u1/concepts, /quiz, /cheatsheet, /exercises — todos cargan sin 404
- npm run build local completa sin errores
- README.md actualizado con informacion real del proyecto
</success_criteria>

<output>
Despues de completar, crear `.planning/phases/04-deploy/04-01-SUMMARY.md` con:
- URL de produccion real
- Estado de verificacion de las 5 rutas
- NFR-04 marcado como complete
</output>
