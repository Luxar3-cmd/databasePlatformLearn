---
phase: 01-fundacion
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/pages/InfoGeneral.tsx
  - src/pages/SectionPage.tsx
  - src/components/ui/PlaceholderSection.tsx
  - src/router.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Sidebar.tsx
autonomous: false
requirements: [LAYOUT-04, LAYOUT-05, LAYOUT-06, NFR-02]

must_haves:
  truths:
    - "El sidebar se colapsa en mobile (<1024px) y se abre como overlay con hamburger"
    - "Al seleccionar seccion en overlay mobile, el sidebar se cierra automaticamente"
    - "La pagina Informacion General (/) muestra presentacion del curso con nombre, profesor, semestre y mini-guia"
    - "Las secciones sin contenido muestran card placeholder con titulo, descripcion e icono"
    - "La URL cambia al navegar entre sub-secciones de U1"
    - "El layout es responsive: en desktop sidebar visible, en mobile oculto"
  artifacts:
    - path: "src/pages/InfoGeneral.tsx"
      provides: "Landing page with course info and platform guide"
      exports: ["default"]
      min_lines: 40
    - path: "src/pages/SectionPage.tsx"
      provides: "Section router that shows placeholder cards"
      exports: ["default"]
    - path: "src/components/ui/PlaceholderSection.tsx"
      provides: "Reusable placeholder card for empty sections"
      exports: ["default"]
  key_links:
    - from: "src/pages/SectionPage.tsx"
      to: "src/data/units.ts"
      via: "looks up unit and section from URL params"
      pattern: "useParams.*unitId.*section"
    - from: "src/pages/SectionPage.tsx"
      to: "src/components/ui/PlaceholderSection.tsx"
      via: "renders placeholder for unimplemented sections"
      pattern: "PlaceholderSection"
    - from: "src/router.tsx"
      to: "src/pages/InfoGeneral.tsx"
      via: "index route renders InfoGeneral"
      pattern: "index.*InfoGeneral"
---

<objective>
Completar la experiencia de navegacion: pagina Informacion General como landing, placeholders para secciones vacias, sidebar responsive con overlay mobile que se cierra al seleccionar, y verificacion visual final del shell completo.

Purpose: El estudiante puede navegar toda la estructura de la plataforma de forma fluida en desktop y mobile. Las secciones vacias comunican que contendran en el futuro.
Output: Shell visual completo y responsive, listo para recibir contenido real en fase 2.
</objective>

<execution_context>
@/home/imorales/.claude/get-shit-done/workflows/execute-plan.md
@/home/imorales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fundacion/01-RESEARCH.md
@.planning/phases/01-fundacion/01-01-SUMMARY.md
@.planning/phases/01-fundacion/01-02-SUMMARY.md

<interfaces>
<!-- Tipos y exports de planes anteriores -->

From src/data/units.ts:
```typescript
export interface UnitSection { id: SectionId; label: string; icon: string; }
export interface Unit { id: string; number: number; title: string; locked: boolean; sections: UnitSection[]; }
export const UNITS: Unit[];
export const SECTIONS: UnitSection[];
```

From src/router.tsx:
```typescript
// Routes:
// index: true -> InfoGeneral
// path: 'unit/:unitId/:section' -> SectionPage
```

From src/components/layout/AppLayout.tsx:
```typescript
// State: isOpen (mobile), collapsed (desktop)
// Passes onCloseMobile to Sidebar
// Passes onOpenMobile to Header
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InfoGeneral + PlaceholderSection + SectionPage</name>
  <files>src/pages/InfoGeneral.tsx, src/pages/SectionPage.tsx, src/components/ui/PlaceholderSection.tsx, src/router.tsx</files>
  <action>
1. Crear `src/components/ui/PlaceholderSection.tsx`:
   - Props: `title: string`, `description: string`, `icon: string` (nombre de lucide icon)
   - Card centrada con:
     - Icono grande (lucide icon renderizado desde mapa, tamano 48px, color zinc-600)
     - Titulo de la seccion (`text-xl font-semibold text-zinc-300`)
     - Descripcion breve de que contendra (`text-zinc-500`)
     - Borde sutil: `border border-zinc-800 rounded-lg` (recordar: en v4 `rounded-lg` es lo que antes era `rounded-md`)
     - Fondo: `bg-zinc-900/50`
     - Padding generoso (p-8 o p-12)
   - Usar el mismo mapa de iconos de lucide que se creo en plan 02

2. Crear `src/pages/SectionPage.tsx`:
   - Usar `useParams` de react-router para obtener `unitId` y `section`
   - Buscar la unidad en `UNITS.find(u => u.id === unitId)`
   - Si no se encuentra la unidad: mostrar mensaje "Unidad no encontrada"
   - Buscar la seccion en `unit.sections.find(s => s.id === section)`
   - Si no se encuentra la seccion: mostrar mensaje "Seccion no encontrada"
   - Si encontrada: renderizar `<PlaceholderSection>` con:
     - title: seccion.label (ej. "Conceptos", "Editor SQL")
     - description: texto descriptivo por seccion (mapa de SectionId a descripcion):
       - conceptos: "Contenido teorico de la unidad con definiciones, tablas comparativas y ejemplos"
       - ejercicios: "Ejercicios practicos resueltos paso a paso"
       - editor-sql: "Editor SQL interactivo para ejecutar consultas contra una base de datos real"
       - quiz: "Preguntas de seleccion multiple para evaluar tu comprension"
       - cheat-sheet: "Resumen visual con los conceptos clave de la unidad"
     - icon: seccion.icon
   - Mostrar el nombre de la unidad como breadcrumb/header arriba del placeholder (ej. "Unidad 1: Introduccion a Bases de Datos > Conceptos")

3. Crear `src/pages/InfoGeneral.tsx`:
   - Pagina de landing con card principal:
     - Titulo: "BDD Lab UTFSM"
     - Subtitulo: "Laboratorio Interactivo — INF-239 Bases de Datos"
     - Informacion del curso en cards o secciones:
       - Profesor: "Por definir" (placeholder generico)
       - Semestre: "2026-1"
       - Descripcion breve del curso: "Plataforma de aprendizaje interactivo para el curso de Bases de Datos de la Universidad Tecnica Federico Santa Maria"
     - Mini-guia de uso (seccion "Como usar esta plataforma"):
       - "Navega por las unidades en el sidebar"
       - "Cada unidad tiene secciones de Conceptos, Ejercicios, Editor SQL, Quiz y Cheat Sheet"
       - "Comienza con la Unidad 1 — las demas se habilitaran progresivamente"
     - Estilo: cards con bordes zinc-800, fondo zinc-900/50, spacing generoso
     - Todo en espanol (NFR-05)

4. Actualizar `src/router.tsx`:
   - Reemplazar los componentes placeholder inline por los imports reales de `InfoGeneral` y `SectionPage`

Todo el texto en espanol. Usar palette dark: fondos zinc-900/50, bordes zinc-800, texto zinc-100/zinc-300/zinc-500 segun jerarquia. Acento indigo-500 para links o elementos destacados.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>La ruta `/` muestra InfoGeneral con presentacion del curso y mini-guia. La ruta `/unit/u1/conceptos` muestra placeholder con titulo "Conceptos", descripcion e icono. Unidad no existente muestra mensaje de error. `npx tsc --noEmit` pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive final — sidebar mobile overlay y ajustes</name>
  <files>src/components/layout/AppLayout.tsx, src/components/layout/Sidebar.tsx</files>
  <action>
1. Verificar y ajustar `src/components/layout/AppLayout.tsx`:
   - El overlay backdrop (div bg-black/50) debe estar SOLO visible en mobile (menor a lg) cuando isOpen=true
   - Click en backdrop cierra sidebar (`onCloseMobile`)
   - El backdrop debe tener `z-30` (debajo del sidebar z-40) y cubrir toda la pantalla
   - Agregar `lg:hidden` al backdrop para que no aparezca en desktop

2. Verificar y ajustar `src/components/layout/Sidebar.tsx`:
   - Asegurar que `onCloseMobile` se llama cuando se selecciona una seccion en mobile (el `onNavigate` prop de SidebarUnit)
   - El `onNavigate` callback solo debe cerrar el sidebar si estamos en mobile. Forma simple: siempre llamar `onCloseMobile` — en desktop el sidebar es `lg:relative lg:translate-x-0` asi que el estado isOpen no afecta.

3. Verificar comportamiento responsive:
   - Desktop (>=1024px): sidebar visible, boton de collapse funcional, no hay overlay
   - Mobile (menor a 1024px): sidebar oculto, hamburger en header lo abre como overlay, click en seccion o backdrop lo cierra
   - Header: sticky en ambos viewports
   - Footer: siempre al final del area de contenido (no del viewport — si el contenido es corto, footer queda al fondo del flex column)

4. Ajustes de polish si es necesario:
   - Transiciones suaves en sidebar (transition-transform duration-200)
   - Focus management: cuando se abre el overlay, podria tener aria-label="Navegacion" para accesibilidad basica
   - El main area no debe scrollear cuando el overlay esta abierto en mobile (opcional: `overflow-hidden` en body cuando isOpen en mobile)

NOTA: Gran parte de esto ya deberia estar implementado en plan 02. Esta task es para verificar, ajustar y pulir el comportamiento responsive. Si plan 02 lo implemento bien, los cambios seran minimos.
  </action>
  <verify>
    <automated>cd /home/imorales/dev/databasePlatformLearn && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>`npm run build` completa sin errores. El sidebar se oculta en mobile y se muestra como overlay al presionar hamburger. Click en seccion del overlay cierra el sidebar y navega. En desktop el sidebar es visible con collapse funcional. Header sticky en ambos viewports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificacion visual del shell completo</name>
  <action>
Verificacion visual del shell completo de BDD Lab UTFSM: sidebar con unidades (U1 activa, U2-U6 bloqueadas), header/footer, pagina InfoGeneral, placeholders de secciones, sidebar responsive con overlay mobile, dark mode permanente.
  </action>
  <verify>Verificacion humana requerida</verify>
  <done>El usuario aprueba la verificacion visual del shell completo</done>
  <what-built>Shell visual completo de BDD Lab UTFSM: sidebar con unidades (U1 activa, U2-U6 bloqueadas), header/footer, pagina InfoGeneral, placeholders de secciones, sidebar responsive con overlay mobile, dark mode permanente.</what-built>
  <how-to-verify>
    1. Ejecutar `npm run dev` y abrir http://localhost:5173
    2. Verificar la pagina de inicio (/): debe mostrar info del curso y mini-guia de uso
    3. En el sidebar: U1 debe tener sub-secciones expandibles, U2-U6 deben estar grayed out con candado
    4. Click en "Conceptos" bajo U1: la URL debe cambiar a /unit/u1/conceptos y mostrar placeholder
    5. Click en una unidad bloqueada (U2-U6): no debe navegar
    6. Verificar Tareas/Certamenes/Ayudantias en seccion separada del sidebar, estilo bloqueado
    7. Header sticky: scrollear la pagina, el header debe quedarse arriba
    8. Footer: "BDD Lab UTFSM - Ayudantia INF-239 - 2026"
    9. Reducir el ancho del browser a menos de 1024px: el sidebar debe desaparecer y aparecer un hamburger en el header
    10. Click en hamburger: sidebar se abre como overlay. Click en una seccion: se cierra y navega
    11. Verificar dark mode: fondo oscuro, texto claro, sin colores rotos
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si todo se ve bien, o describe los problemas que encuentres</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completa sin errores ni warnings criticos
2. Ruta `/` muestra InfoGeneral con info del curso
3. Ruta `/unit/u1/conceptos` muestra placeholder con titulo e icono
4. Las 5 sub-secciones de U1 son navegables con URL unica cada una
5. Sidebar responsive: visible en desktop, overlay en mobile
6. Overlay se cierra al seleccionar seccion
7. Dark mode consistente en toda la plataforma
8. Todo el texto en espanol
</verification>

<success_criteria>
El estudiante puede navegar la estructura completa de la plataforma en desktop y mobile. Ve sidebar con U1 activa y U2-U6 bloqueadas, navega sub-secciones, ve placeholders informativos, y la pagina de Informacion General como landing. Todo en dark mode profesional y en espanol.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacion/01-03-SUMMARY.md`
</output>
